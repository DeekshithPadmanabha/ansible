- name: Demo on map to list
  hosts: local
  connection: local
  vars:
    tags:
      name: ansible
      project: roboshop
      component: catalogue

  tasks:
  - name: convert map to list
    ansible.builtin.debug:
      msg: "{{ tags | dict2items }}"  